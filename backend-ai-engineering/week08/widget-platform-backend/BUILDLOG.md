# AI Build Log

* **Design Phase:** Used AI to brainstorm the JSONB structure for storing flexible form fields and geo-location data.
* **Troubleshooting:** AI helped me resolve a `FATAL: password authentication failed` error by correctly setting up the `.env` configuration for Spring Boot.
* **Code Generation:** Used AI to generate the boilerplate for the `GlobalExceptionHandler` and the Bucket4j `RateLimitFilter`. I reviewed the code and adjusted the rate limit threshold to match the capstone requirements.
* **Testing:** AI assisted in writing the MockMvc automated tests for validating the CORS preflight headers and honeypot logic.